apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecrets.stripe.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "external-secrets.labels" . | nindent 4 }}
    secret-type: payment-keys
spec:
  refreshInterval: {{ .Values.externalSecrets.stripe.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: SecretStore
  target:
    name: {{ .Values.externalSecrets.stripe.secretName }}
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        {{- range .Values.externalSecrets.stripe.keys }}
        {{ .key }}: "{{ `{{ .` }}{{ .property }}{{ ` }}` }}"
        {{- end }}
  data:
    {{- range .Values.externalSecrets.stripe.keys }}
    - secretKey: {{ .property }}
      remoteRef:
        key: {{ $.Values.externalSecrets.stripe.awsSecretName }}
        property: {{ .property }}
    {{- end }}