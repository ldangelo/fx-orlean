apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecrets.keycloak.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "external-secrets.labels" . | nindent 4 }}
    secret-type: keycloak-config
spec:
  refreshInterval: {{ .Values.externalSecrets.keycloak.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: SecretStore
  target:
    name: {{ .Values.externalSecrets.keycloak.secretName }}
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        {{- range .Values.externalSecrets.keycloak.keys }}
        {{ .key }}: "{{ `{{ .` }}{{ .property }}{{ ` }}` }}"
        {{- end }}
  data:
    {{- range .Values.externalSecrets.keycloak.keys }}
    - secretKey: {{ .property }}
      remoteRef:
        key: {{ $.Values.externalSecrets.keycloak.awsSecretName }}
        property: {{ .property }}
    {{- end }}