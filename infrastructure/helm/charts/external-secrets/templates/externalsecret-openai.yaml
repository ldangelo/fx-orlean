apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecrets.openai.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "external-secrets.labels" . | nindent 4 }}
    secret-type: api-keys
spec:
  refreshInterval: {{ .Values.externalSecrets.openai.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: SecretStore
  target:
    name: {{ .Values.externalSecrets.openai.secretName }}
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        {{- range .Values.externalSecrets.openai.keys }}
        {{ .key }}: "{{ `{{ .` }}{{ .property }}{{ ` }}` }}"
        {{- end }}
  data:
    {{- range .Values.externalSecrets.openai.keys }}
    - secretKey: {{ .property }}
      remoteRef:
        key: {{ $.Values.externalSecrets.openai.awsSecretName }}
        property: {{ .property }}
    {{- end }}