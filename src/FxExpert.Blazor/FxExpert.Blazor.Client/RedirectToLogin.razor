@inject NavigationManager NavigationManager

@code {

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/signin-oidc?ReturnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }

}